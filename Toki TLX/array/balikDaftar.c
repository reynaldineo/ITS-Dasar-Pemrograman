#include <stdio.h>
int main () {
    int x[101];
    int n=0;
    for (int i ; i != EOF; i++)
    {
        scanf("%d", &x[i]);
        n++;
    }
    
     for (n-1 ; n = 0; n--)
    {
        printf("%d", x[i]);
    }

    return 0;
}