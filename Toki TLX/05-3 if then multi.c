#include <stdio.h>

    int main (){

        int angka;

            scanf("%d", &angka);

            if (angka % 2 == 0 && angka >0)
            {
                printf("%d", angka);
            }   
              
        return 0;
    }