#include <stdio.h>

int powOf2 (int n) {
   int angka = n * n; 
   
  // return angka;
}

int main (){
    int u;
    scanf("%d", &u);
    
    printf("%d", powOf2(u)); // powOf2 nya harus ada (parameter)

    return 0;
}