#include <stdio.h>

int faktorial(int input) {
  if(input > 1)
    return input * hitungFactorial(input - 1);
  else
    return 1;
}

int main () {

    int n, k;
    scanf("%d", &n, &k);

    int hasil;
    hasil = 

    return 0;
}