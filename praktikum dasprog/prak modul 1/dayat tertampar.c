#include <stdio.h>

    int main () {

        // I 1, V 5, X 10, L 50, C 100

        // int I = 1;
        // int V = 5;
        // int X = 10;
        // int L = 50;
        // int C = 100;

       char I, V, X, L, C;

        
        // dari I + V X L C
        if ((scanf ("%c %c"), &I, &V) == IV || (scanf ("%c %c"), &V, &I) )
        {printf ("6");}

        else if ((scanf ("%c %c"), &I, &X) || (scanf ("%c %c"), &X, &I) )
        {printf ("11");}

        else if ((scanf ("%X %d"), &I, &L ) || (scanf ("%X %d"), &L, &I))
        {printf ("51");}

        else if ((scanf ("%X %d"), &I, &C ) || (scanf ("%X %d"), &I, &X) )
        {printf ("101");}

        
        





        return 0;
    }