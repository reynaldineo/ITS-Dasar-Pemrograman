#include <stdio.h>

int cek (int dango[], int pendaftar, int tepat) {

    if (cek == tepat) {
        return cek;
    }
    return cek + cek(dango[pendaftar-1]);
}


int main () {

    int pendaftar, butuhBrp;
    int dango[];
    int tepat;

    scanf("%d %d", &pendaftar, &butuhBrp);

    for (int i = 0; i < pendaftar; i++)
    {
        scanf("%d", &arr[i]);
    }

    for (int i = 0; i < butuhBrp; i++)
    {
        if (arr[i] + arr[i+1]   == tepat)
        {
            printf("KoSimp boleh pulang")
        }
    }

    return 0;
}
