#include <stdio.h> 

int main () {

    int ulang, detik;
    scanf("%d %d", &ulang, &detik);

    int serang[];

    for (int i = 0; i < ulang; i++) //input loop
    {
        scanf("%d", &serang[i])
    }
    
    for (size_t i = 0; i < count; i++) //ngecek isi
    {
        /* code */
    }
    

    return 0;
}